<?xml version="1.0" encoding="utf-8"?>
<!--
  Small solid dot used as the Record button's record indicator - red (history_accent) regardless
  of theme, per the reviewed mockup.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/history_accent" />
    <size
        android:width="10dp"
        android:height="10dp" />
</shape>
